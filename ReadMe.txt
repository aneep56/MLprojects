1.Install Python in VScode. Works with 3.11 version of python.

2.Install following pip via command prompt:

       pip install opencv-python

       pip install mediapipe

       pip install pycaw

       pip install Pillow
       
       pip install tk

Now you are good to go. Just run the code